# PWA 呼叫本機 Cloudflare Worker 的公開 API 位址。
VITE_API_BASE_URL=http://127.0.0.1:8787

# 正式部署時由 Cloudflare 設定，不要提交 secrets。
CORS_ORIGIN=https://ticket-radar.example.com
ALLOW_DEMO_AUTH=false

# Staging PWA 建置：
# VITE_API_BASE_URL=https://ticket-radar-api-staging.<workers-subdomain>.workers.dev
#
# Worker Staging 由 wrangler env.staging 設定：
# ACCESS_TEAM_DOMAIN=<team>.cloudflareaccess.com
# ACCESS_AUD=<Cloudflare Access Application Audience tag>
# RATE_LIMIT_SALT 使用 `wrangler secret put`，不得寫入本檔或版本庫。
