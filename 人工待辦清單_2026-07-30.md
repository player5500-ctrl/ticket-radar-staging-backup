# 追票雷達 Ticket Radar — 人工待辦清單（2026-07-30）

Cowork 這邊能做的驗收都做完了。以下是只有你（或有實體裝置/帳號權限的人）能完成的項目，依急迫程度排序。

---

## 需要盡快處理

### 1. Extension 實機載入與操作測試
Cowork 的瀏覽器自動化工具無法操作作業系統原生的「選擇資料夾」對話框，所以「載入未封裝擴充功能」這一步只能你親自做。

**步驟**：
1. 打開 Chrome，網址列輸入 `chrome://extensions`
2. 右上角開啟「開發人員模式」
3. 點「載入未封裝項目」，選擇 `apps/extension/dist`（或對應 build 產物路徑）
4. 到 Demo 售票頁（`Ticket Radar Demo` 標示的頁面）走一次完整流程：開啟→選資料→填入→送出→偵測成功頁→遮罩→截圖→建立紀錄
5. 特別注意：遮罩截圖的實際效果（`captureVisibleTab`）、下載提示是否正常

**為什麼重要**：這是 STAGING_ACCEPTANCE_CHECKLIST.md 裡唯一还完全没有实机验证过的功能區塊。

---

### 2. 清理 exFAT 損壞目錄
`apps/web/.wrangler/tmp/bundle-7jiIjt` 已損壞（連 PowerShell 的 `Remove-Item -Force` 都無法刪除，Access denied）。

**步驟**：
1. 找方便重開機的時間
2. 關閉所有正在使用 D: 磁碟的程式（Claude Code、檔案總管等）
3. 執行 `chkdsk D: /f`
4. 依提示排程下次開機檢查，或強制卸載磁碟

**不急**：不影響部署或功能，只影響 `prettier --check .` 這個檢查指令會報 EPERM。

---

## 可以排程處理

### 3. 設定第一個 Admin 帳號
目前沒有任何帳號有 admin 權限，導致管理後台功能完全無法驗收。

**步驟**：需要人工對 D1 執行 SQL：
```sql
UPDATE users SET role = 'admin' WHERE email = '<你要指定的 email>';
```
用 `wrangler d1 execute ticket-radar-db-staging --env staging --remote --command "..."` 執行。

**為什麼要人工做**：系統設計上沒有自助升級成 admin 的路徑（安全考量），這是刻意的。

---

### 4. Session 自然過期畫面驗證
需要等 Access session 自然過期（目前設定 6 小時），或者手動讓 JWT 失效後測試畫面反應。

**簡化做法**：把 Access Application 的 Session Duration 暫時調短（例如 5 分鐘），測完再調回來。位置：Zero Trust → Access → Applications → ticket-radar-web-staging → Settings → Session Duration。

---

## 之後有空再看

### 5. 手機實機安裝 PWA
Cowork 已確認 manifest、圖示、Service Worker 都符合技術上的可安裝條件，但實際在手機瀏覽器點「加入主畫面」看真實效果如何，需要真機測試。

### 6. 遮罩截圖實際使用者體驗
`background.ts` 的 `prepare-capture` 確認流程邏輯上存在，但實際操作起來順不順手，需要跟 Extension 實機測試（項目 1）一起做。

### 7. TASK-06 文件債
`docs/ARCHITECTURE.md` 承諾了 `/me/export` 和 CSRF 雙重檢查機制，但都還沒實作。要嘛補實作，要嘛先把文件改成符合現況——目前算「說了但沒做」的落差，不影響功能但會誤導後續維護的人。

---

## 已經不用管的

- ~~TASK-01~05~~：已修復並在真實 Staging 驗收通過
- ~~使用者隔離~~：已用第二組帳號驗證通過
- ~~Rate Limit~~：已驗證正確運作
- ~~PWA manifest/icons/SW~~：已驗證符合條件

---

## 總結

目前 Staging 已經非常接近 Ready，只差 Extension 實機驗證（項目 1）跟 admin 帳號設定（項目 3）這兩項需要人工，其他都是收尾性質。建議先做項目 1，因為它是唯一完全沒驗證過的功能區塊。
