# PWA 呼叫本機 Cloudflare Worker 的公開 API 位址（僅本機開發使用）。
VITE_API_BASE_URL=http://127.0.0.1:8787

# 正式部署時由 Cloudflare 設定，不要提交 secrets。
CORS_ORIGIN=https://ticket-radar.example.com
ALLOW_DEMO_AUTH=false

# Staging／Production PWA 建置：
#   不要設定 VITE_API_BASE_URL（或設成空字串）。
#   API 一律走 Pages 同源路徑 /api/*，由 apps/web/functions/api/[[path]].ts 轉發到 Worker；
#   設成跨網域的 https://...workers.dev 會被前端忽略並記一筆 console 警告，因為跨網域
#   會再次觸發第二個 Cloudflare Access Application 的互動式登入，讓所有 API 卡死。
#
# Cloudflare Pages 專案（ticket-radar-web-staging）需要設定其中一項：
#   1.（建議）Settings → Functions → Service bindings：變數名 API → 綁 ticket-radar-api-staging
#   2. 環境變數 API_ORIGIN=https://ticket-radar-api-staging.<workers-subdomain>.workers.dev
#      走這條的話，API 網域上的 Access Application 必須先移除或改成允許 Service Token。
#
# Worker Staging 由 wrangler env.staging 設定：
# ACCESS_TEAM_DOMAIN=<team>.cloudflareaccess.com
# ACCESS_AUD=<Pages Access Application AUD>,<API Access Application AUD>（可逗號分隔多個）
# RATE_LIMIT_SALT 使用 `wrangler secret put`，不得寫入本檔或版本庫。
