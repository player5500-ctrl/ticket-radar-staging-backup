# 交辦：修復 prototype 頁面內部連結錯誤前綴導致 404（2026-08-03，第 1 輪）

## 已定案決策（Vanny）

追票雷達 Ticket Radar Staging 環境在 TASK-01~05 修復並驗收通過後，網站被重新部署成一套新的「prototype」風格 UI（霓虹主題、含「作戰模式」等功能）。這套新 UI 已經是目前 Staging 實際運作的版本，不是要拔掉的東西——只是這套新 UI 裡面，很多內部連結／導頁邏輯還留著開發階段用的 `/prototype` 路徑前綴，但實際路由設定並沒有這個前綴，導致點下去會 404。這次只修這個路徑前綴不一致的問題，不要改版面、不要改功能邏輯、不要動 UI 風格。

## 背景與現場

- 專案路徑：`D:\cowork files\projects\18_ticket-radar`（monorepo，沒有 `.git` 歷史，這次修改後請用「修改檔案清單＋逐檔案一句話」交代改了什麼，不要嘗試 `git init`）。
- 前端：`apps/web`，React + Vite + React Router（`react-router-dom`）+ TanStack Query，PWA。
- Staging 網址（真實環境，不是本機模擬）：`https://ticket-radar-web-staging.pages.dev`。
- 路由設定檔：`apps/web/src/app/router.tsx`。目前這個檔案把一套叫做「Proto*」的頁面元件（來自 `apps/web/src/prototype/pages/` 目錄）直接掛在**沒有任何前綴的根路徑**上，實際路由對照如下（這是唯一真正生效的路由表，以此為準）：

  | 路徑 | 元件 |
  |---|---|
  | `/` | `ProtoHomePage` |
  | `/search` | `ProtoSearchPage` |
  | `/tasks` | `ProtoTasksPage` |
  | `/battle` | `ProtoBattleModePage` |
  | `/records` | `ProtoRecordsPage` |
  | `/events/:eventId` | `ProtoDetailPage` |
  | `/demo-ticket` | `DemoTicketPage`（非 prototype，另一套） |
  | `/admin` | `AdminPage`（有 `AdminGuard` 保護） |
  | `*`（其餘） | `NotFoundPage` |

  這個路由表裡面**完全沒有 `/prototype` 這個路徑前綴**，也沒有獨立的 `/prototype/*` 子路由。

- **問題根源**：`apps/web/src/prototype/` 目錄下，這幾個檔案裡的內部導頁邏輯（`navigate(...)` 呼叫）與連結（`<Link to="...">`）還是寫死用 `/prototype/xxx` 這種帶前綴的路徑，但實際路由表（上面那張表）裡對應的路徑是不帶 `/prototype` 前綴的。使用者只要點到這些連結／觸發這些導頁，就會被導向一個路由表裡不存在的路徑，命中 `*` → `NotFoundPage`，畫面顯示 404。

  這是在一次已驗收通過的真實 Staging 測試中發現的：使用者在 `/events/:eventId` 頁面成功建立購票任務（後端正確回傳 201、資料確實寫入），但送出後前端呼叫了 `navigate("/prototype/tasks")`，導致跳轉到 404 頁面，即使資料其實已經正確存在於 `/tasks` 頁面。

- **已確認為死碼、不在本次修復範圍內**：`apps/web/src/prototype/PrototypeLayout.tsx` 這個檔案定義了一整套帶 `/prototype` 前綴的 side nav（`/prototype`、`/prototype/search`、`/prototype/tasks`、`/prototype/battle`、`/prototype/records`），但經過全 repo 搜尋確認，**這個元件沒有被任何地方 import 或使用**（只有它自己內部定義），不會被渲染到畫面上。這個檔案本身的路徑前綴問題不影響任何使用者，本次不用修改它；如果你想額外處理，請先在回報中列出來讓 Vanny 決定要不要保留或刪除，不要自己直接刪檔案。

- **已確認需要修正的檔案與行號**（全 repo 搜尋 `/prototype` 字串比對路由表後的完整清單，逐一列出「現在寫的路徑」→「應該改成的路徑」）：

  1. `apps/web/src/prototype/pages/ProtoHomePage.tsx:39`
     `navigate(val ? \`/prototype/search?q=${encodeURIComponent(val)}\` : "/prototype/search")`
     → 改成 `navigate(val ? \`/search?q=${encodeURIComponent(val)}\` : "/search")`

  2. `apps/web/src/prototype/pages/ProtoHomePage.tsx:56`
     `onClick={() => navigate("/prototype/search")}`
     → 改成 `onClick={() => navigate("/search")}`

  3. `apps/web/src/prototype/pages/ProtoHomePage.tsx:103`
     `onClick={() => navigate(\`/prototype/search?q=${encodeURIComponent(artist.name)}\`)}`
     → 改成 `onClick={() => navigate(\`/search?q=${encodeURIComponent(artist.name)}\`)}`

  4. `apps/web/src/prototype/pages/ProtoHomePage.tsx:137`
     `<Link to="/prototype/search" ...>`
     → 改成 `<Link to="/search" ...>`

  5. `apps/web/src/prototype/pages/ProtoHomePage.tsx:183`
     `onClick={() => navigate("/prototype/battle")}`
     → 改成 `onClick={() => navigate("/battle")}`

  6. `apps/web/src/prototype/pages/ProtoDetailPage.tsx:39`
     `navigate("/prototype/tasks");`
     → 改成 `navigate("/tasks");`
     （這是使用者實測發現的那個 bug：建立購票任務成功後導頁 404）

  7. `apps/web/src/prototype/pages/ProtoDetailPage.tsx:74`
     `<Link to="/prototype/search" ...>`
     → 改成 `<Link to="/search" ...>`

  8. `apps/web/src/prototype/pages/ProtoDetailPage.tsx:87`
     `<Link to="/prototype/search" ...>`
     → 改成 `<Link to="/search" ...>`

  9. `apps/web/src/prototype/components/ProtoEventCard.tsx:50`
     `to={\`/prototype/event/${event.id}\`}`
     → 改成 `to={\`/events/${event.id}\`}`
     **注意這條除了前綴要拿掉，路徑片段本身也要從單數 `event` 改成複數 `events`**，因為路由表裡對應的是 `/events/:eventId`（見上面那張表），不是 `/event/:eventId`。這條如果只拿掉前綴、沒改單複數，一樣會 404。

  10. `apps/web/src/prototype/pages/ProtoBattleModePage.tsx:43`
      `<Link to="/prototype" ...>`
      → 改成 `<Link to="/" ...>`

- 以上 10 處是用 grep 對 `apps/web/src/prototype/` 整個目錄搜尋 `navigate(` 與 `to="/` 、`` to={`/ `` 之後，逐一比對路由表得出的完整清單。`ProtoSearchPage.tsx`、`ProtoTasksPage.tsx`、`ProtoRecordsPage.tsx` 這三個檔案經搜尋確認沒有類似的 `/prototype` 前綴路徑，不需要修改。但請你自己動手前，**再重新對 `apps/web/src/prototype/` 整個目錄跑一次 `/prototype` 字串搜尋**（例如 `grep -rn "/prototype" apps/web/src/prototype/`），確認沒有本清單遺漏的第 11 處——如果 grep 找到清單外的新命中，一併比對路由表修正，並在回報中列出你額外修了什麼。

## 硬約束（全文寫出，不引用外部檔）

- 只修路徑前綴／路徑片段不一致的問題（即上面 10 處＋你 grep 覆核後可能發現的遺漏），**不要**改版面、不要改樣式、不要改功能邏輯、不要動這套「prototype」風格 UI 本身的設計。
- **不要刪除** `apps/web/src/prototype/PrototypeLayout.tsx`，即使你確認它是死碼；只能在回報中提出建議，讓 Vanny 決定。
- 不碰 Production 環境、不執行任何 `wrangler deploy --env production`、不要嘗試登入 Cloudflare 帳號或改任何 Cloudflare 後台設定。
- 不改 D1 migration、不改後端 `workers/api` 任何檔案（這個 bug 純粹是前端字串路徑問題，後端已確認邏輯正確，任務資料確實有正確寫入 D1）。
- 不新增套件依賴。
- 這次沒有 `.git`，請不要嘗試 `git init` 或建立版本控制。
- 完成後不要自己宣稱「已驗收通過」，只能說「已自測：<結果>」，正式驗收會由 Vanny 或另一個 fresh context 的 agent 執行。

## 完成標準（量化，逐條可打勾）

- [ ] 上述 10 處（或加上你 grep 覆核後發現的遺漏處）全部改成不帶 `/prototype` 前綴、且路徑片段（單複數）與 `apps/web/src/app/router.tsx` 裡的路由表完全一致。
- [ ] `pnpm --filter @ticket-radar/web build` 通過，無 TypeScript 型別錯誤。
- [ ] 本機起 `pnpm --filter @ticket-radar/web dev`，手動點過至少這幾條路徑確認不再 404：首頁搜尋框送出搜尋 → 進到搜尋頁；某個活動卡片點進去 → 進到 `/events/:id` 詳情頁（不是 404）；在詳情頁模擬建立任務後導頁 → 進到 `/tasks`（不是 404）；首頁「作戰模式」相關按鈕 → 進到 `/battle`。
- [ ] 除了上述修改，`git diff`（若有版控）或改動清單裡沒有其他非必要變動（不夾帶格式化整份檔案、不夾帶功能修改）。

## 回報格式（請照此回報）

```
## 已改成
- <逐項一句話，含檔案路徑:行號，例：ProtoHomePage.tsx:39 navigate 目標從 /prototype/search 改成 /search>

## 額外發現（若 grep 覆核找到清單外的遺漏處）
- <path:行號 ＋ 現在是什麼 ＋ 改成什麼>（沒有就寫「無」）

## 自測結果
- build：<pass/fail>
- 本機手動點擊路徑驗證：<逐條列出你點了什麼、結果是什麼>

## 未做項目
- <明列 + 原因>（沒有就寫「無」）

## 改動檔案清單
- <path 清單，每個檔案一句話說明改了什麼>
```

## 狀態

待處理 → （Codex 回報後由 Claude/Vanny 更新：驗收中／已完成／退回第 2 輪）

<!-- ✂ 檔尾哨兵｜本檔至此完整結束 -->
